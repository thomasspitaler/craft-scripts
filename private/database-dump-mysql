#! /usr/bin/env bash

#read coniguration
source $(dirname $0)/../.env

# dump database to stdout
extra_params=""
# extra_params="--column-statistics=0"
mysqldump -h"${DB_SERVER:-localhost}" --port=${DB_PORT:-3306} -u"$DB_USER" -p"$DB_PASSWORD" --databases "$extra_params" "$DB_DATABASE"
